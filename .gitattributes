# Ensure gradlew shell script uses LF line endings everywhere
gradlew text eol=lf

# Ensure binary files are not corrupted by line ending conversion
*.jar binary
*.png binary
*.jpg binary
*.p12 binary
*.keystore binary
